function love.conf(t)
    t.window.width = 1201    -- Set the window width to 800 pixels
    t.window.height = 800   -- Set the window height to 600 pixels
    t.window.title = "My LÖVE Game"  -- Set the window title
    t.window.highdpi = true
    t.console = false
    -- You can add more configurations here if needed
end
